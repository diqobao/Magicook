import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  navContainer: {
    flexDirection: 'row',
    // position: 'absolute',
    alignSelf: 'center',
    justifyContent: 'center',
    top: 680,
  },
  MessageBox: {
    // position: 'absolute',
    alignSelf: 'center',
    justifyContent: 'center',
    top: 250,
  },
})